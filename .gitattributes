# Windows batch files must use CRLF
*.bat text eol=crlf

# Shell scripts must use LF
*.sh text eol=lf
*.command text eol=lf

# Shogunate package releases currently ship the tmux runtime only.
android/ export-ignore
